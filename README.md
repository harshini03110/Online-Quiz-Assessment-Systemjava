## Run the Project
javac MyQuizProject.java && java MyQuizProject  
# quiz data stored in quizfile.txt

## Push to GitHub

git init && git add . && git commit -m "quiz project" && git branch -M main && git remote add origin https://github.com/your-username/your-repo-name.git && git push -u origin main

## Update Changes

git add . && git commit -m "update" && git push
